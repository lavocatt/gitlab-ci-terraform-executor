version: 0.2

env:
  variables:
    AWS_REGION: us-east-1

phases:
  install:
    commands:
      - terraform version
  build:
    commands:
      - terraform init -lock-timeout=300s
      - terraform plan -lock-timeout=300s
      - terraform deploy -auto-approve -lock-timeout=300s
